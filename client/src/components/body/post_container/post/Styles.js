import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    root: {
      margin: 'auto',
      maxWidth: '50%',
    },
    post_card: {
      margin: '16px',
    }
  });